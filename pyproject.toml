[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "forgecad"
version = "0.0.1"
description = "Open source parametric fabrication tools for FreeCAD"
requires-python = ">=3.11"
